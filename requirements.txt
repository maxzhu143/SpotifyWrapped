asgiref==3.8.1
certifi==2024.8.30
charset-normalizer==3.4.0
coverage==7.6.4
Django==5.1.2
idna==3.10
iniconfig==2.0.0
packaging==24.1
pluggy==1.5.0
pytest==8.3.3
pytest-django==4.9.0
python-decouple==3.8
python-dotenv==1.0.1
requests==2.32.3
sqlparse==0.5.1
urllib3==2.2.3

# Core Django dependencies
Django==4.2  # Use your specific Django version
djangorestframework==3.14.0  # Optional, for building APIs if needed

# Authentication-related dependencies (optional)
django-allauth==0.52.0  # For social authentication like Spotify, if applicable

# Testing and coverage
pytest==7.4.0
pytest-django==4.5.2
coverage==7.3.1  # For code coverage reports
pytest-cov==4.1.0  # Integrates coverage with pytest

# Linting and code style
pylint==2.17.5  # For code linting
black==23.7.0  # Optional, for automatic code formatting

# Database
psycopg2==2.9.7  # Only if using PostgreSQL; omit if using SQLite

# Environment management
python-dotenv==1.0.0  # For managing environment variables, useful in development

# HTTP requests (for APIs, if needed)
requests==2.31.0  # If making external requests, such as to the Spotify API
